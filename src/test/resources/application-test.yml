server:
  port: 0   # random port for tests

# Reuse the jOOQ codegen DB — Flyway already applied V1 during generate-sources.
# Tests clean up their own data in @BeforeEach.

sync:
  garmin:
    sync:
      initial-fetch-days: 7
